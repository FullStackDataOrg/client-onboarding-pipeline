name: 'client_onboarding'
version: '1.0.0'
config-version: 2

profile: 'media_analytics'

model-paths: ["models"]
snapshot-paths: ["snapshots"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  client_onboarding:
    bronze:
      +materialized: view
      +schema: bronze
    silver:
      +materialized: table
      +schema: silver
    gold:
      +materialized: table
      +schema: gold

snapshots:
  client_onboarding:
    +target_schema: snapshots

seeds:
  client_onboarding:
    +schema: bronze
